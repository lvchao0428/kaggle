# planet-wars 目录索引（维护用）

以下目录均配有 `README.md`（本文件不重复写明细，只列树）。

```
planet-wars/
├── README.md              # 总览 + Orbit Wars 对照
├── README                  # 上游无扩展名说明（竞赛时代）
├── README-LISP             # Lisp / 工具链说明
├── planet-wars.asd       # ASDF 系统定义
├── Makefile configure     # 构建
├── MyBot.lisp RunMyBot.lisp ProxyBot.lisp setup.lisp
├── bin/                   # 本地/评估脚本
├── docs/                  # 自动生成符号表 + 本索引
├── example_bots/          # Java 示例 Bot
├── games/                 # 保存的示例对局
├── maps/                  # 地图文本（大量 finals）
├── scripts/               # gen_lisp_symbol_docs.py
├── src/                   # 冠军 Bot + 捆绑依赖
├── tools/                 # 官方 Java JAR
└── …
```

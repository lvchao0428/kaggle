# `src/split-sequence/split-sequence.lisp`

由 `scripts/gen_lisp_symbol_docs.py` 自动生成。

| 行号 | 类型 | 符号 | 说明 |
|------|------|------|------|
| 61 | `DEFUN` | `split-sequence` | （未附带 docstring，见该行附近源码。） |
| 115 | `DEFUN` | `split-sequence-if` | （未附带 docstring，见该行附近源码。） |
| 166 | `DEFUN` | `split-sequence-if-not` | （未附带 docstring，见该行附近源码。） |
| 219 | `DEFUN` | `partition` | （未附带 docstring，见该行附近源码。） |
| 222 | `DEFUN` | `partition-if` | （未附带 docstring，见该行附近源码。） |
| 225 | `DEFUN` | `partition-if-not` | （未附带 docstring，见该行附近源码。） |

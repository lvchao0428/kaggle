# `planet-wars/src/split-sequence/`

[**split-sequence**](https://github.com/sharplispers/split-sequence)：把一个 **sequence** 按分隔判定 **拆成多段**，类似 "`split`" 语义，带 `:count` `:remove-empty-subseqs` 等关键字——引擎读入回合文本时常用。

ASDF `:depends-on #:split-sequence`；逐符号参见 [`../../docs/symbols/by-file/` 下文件名含 `split-sequence`](../../docs/symbols/INDEX.md)。
